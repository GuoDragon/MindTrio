# 核心框架（必需）
mindnlp==0.5.1
mindspore==2.7.0

# Transformers和模型相关（必需）
transformers==4.45.0
accelerate>=1.6.0
peft>=0.7.0

# 数据处理（必需）
datasets==2.18.0
pandas==2.2.0
numpy==1.26.0

# 可选依赖（如不需要可删除以下部分）
# Web服务相关（仅用于模型部署服务）
# flask==2.3.2
# werkzeug==2.3.7
# mysql-connector-python==8.0.26

# 工具库（仅用于特定功能）
# tabulate==0.9.0
# scipy==1.13.0

